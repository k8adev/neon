/**
 * Settings.
 * Basic settings to project style.
 */
$path = '/assets';

rem-calc($size) {
  /**
   * 16 is default font-size.
   */
  $size = unit($size, '') / 16;
  $size = unit($size, 'rem');

  return $size;
}
/**
 * Resets default styles from browser.
 */
@import 'reset'
/**
 * Requires settings and rules.
 */
@import 'colors'
@import 'typography'
@import 'layout'
/**
 * Import all components that you need
 * through the atomic/atomic.styl file.
 */
@import 'atomic/atomic'
